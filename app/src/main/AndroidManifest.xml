<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.fizzbuzz">

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_iconn"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_iconn_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FIZZBUZZ"
        tools:targetApi="31">
        <activity
            android:name=".presentation.second_screen.ResultActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.first_screen.MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>